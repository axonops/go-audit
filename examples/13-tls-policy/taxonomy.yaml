version: 1

categories:
  security:
    - auth_login
  write:
    - user_create

events:
  auth_login:
    description: "User authenticated successfully"
    fields:
      outcome:
        required: true
      actor_id:
        required: true
  user_create:
    description: "A new user account was created"
    fields:
      outcome:
        required: true
      actor_id:
        required: true
