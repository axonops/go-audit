version: 1
app_name: test-app
host: test-host
outputs:
  console:
    type: stdout
  audit_log:
    type: file
    file:
      path: "${AUDIT_TEST_DIR}/audit.log"
    route:
      include_categories:
        write: {}
        security: {}
